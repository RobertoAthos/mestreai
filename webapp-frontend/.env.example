# Base URL of the Mestre IA backend (FastAPI).
#   Local backend     → http://localhost:8000
#   Hosted backend    → https://mestre-api.beto.digital
# The value is read at build time and on the client, so it must be public.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
